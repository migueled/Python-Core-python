## Course
```
math.factorial(5)
math.factorial(n) / ( math.factorial(k) * math.factorial(n -k) )

from math import factorial as fac
fac(n) / ( fact(k) - fac(n - k) )

fac(n) // ( fact(k) - fac(n - k) )

2**31-1
fact(13) > 2**31-1
fact(100)

_

import this

```